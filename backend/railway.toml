# hawk-production: set Railway root directory to `backend` so this file applies.
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
# Uvicorn runs lifespan (init_db) before accepting connections; 30s was too tight for cold start + DB.
healthcheckTimeout = 120
