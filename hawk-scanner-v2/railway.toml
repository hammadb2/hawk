# Web service can use /health; arq worker has no HTTP — disable deploy healthcheck for worker.
# Service B (worker): start command `arq app.queue.worker.WorkerSettings`

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = ""
healthcheckTimeout = 0
